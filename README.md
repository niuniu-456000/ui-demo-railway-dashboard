# UI Demo · Railway Dashboard

独立的铁路数据大屏验证仓库。

- 不修改你的作品集仓库。
- GitHub Pages 路径已经固定为 `/ui-demo-railway-dashboard/`。
- GitHub Actions 部署文件已包含。
- 后续只需要替换 `src/generated/` 下的 Dashboard / data / styles 即可继续验证 Skill。
